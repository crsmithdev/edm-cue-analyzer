{
  "name": "EDM Cue Analyzer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "mounts": [
    "source=/mnt/c/Music/Library,target=/music,type=bind,consistency=cached"
  ],
  "postCreateCommand": "pip install -e .[dev]",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}